{
    "configurations": [
        {
            "type": "gdb",
            "request": "attach",
            "name": "Debug",
            "executable": "${workspaceRoot}/bin/proj_sys_rpi_app",
            "target": "${env:RPI_ADDRESS}:9999",
            "remote": true,
            "gdbpath": "/usr/bin/gdb-multiarch",
            "cwd": "${workspaceRoot}"
        }
    ]
}